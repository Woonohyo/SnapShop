<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginLeft="@dimen/sign_in_email_margin_left_right"
    android:layout_marginRight="@dimen/sign_in_email_margin_left_right"
    android:background="@color/white"
    android:minHeight="250dp"
    android:minWidth="1000dp"
    android:orientation="vertical"
    android:paddingBottom="@dimen/sign_in_email_padding_top_bottom"
    android:paddingLeft="@dimen/sign_in_email_padding_left_right"
    android:paddingRight="@dimen/sign_in_email_padding_left_right"
    android:paddingTop="@dimen/sign_in_email_padding_top_bottom" >

    <TextView
        android:id="@+id/textView_description"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="start"
        android:text="Sign in with your email to see the amazing new things you&apos;ve missed!"
        android:textColor="@color/black" />

    <EditText
        android:id="@+id/editText_email"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:ems="10"
        android:hint="Email Address"
        android:inputType="textEmailAddress"
        android:textColor="@color/black"
        android:textColorHint="@color/hintColorGray" >

        <requestFocus />
    </EditText>

    <EditText
        android:id="@+id/editText_password"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:ems="10"
        android:hint="Password"
        android:inputType="textPassword"
        android:textColor="@color/black"
        android:textColorHint="@color/hintColorGray" />

    <Button
        android:id="@+id/button_sign_in"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/snap_green"
        android:text="Sign In"
        android:textColor="@color/white" />

</LinearLayout>